name: BehaviorDiff blast radius

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  behaviordiff:
    runs-on: ubuntu-24.04
    env:
      BEHAVIORDIFF_WORK: ${{ github.workspace }}/../behaviordiff-work
      BEHAVIORDIFF_FINDINGS: ${{ github.workspace }}/.behaviordiff/artifacts/findings.json
      BEHAVIORDIFF_GATE: warn-only

    steps:
      - name: Check out complete history
        uses: actions/checkout@v4
        with:
          # BehaviorDiff creates worktrees at github.event.pull_request.base.sha and head.sha and
          # calculates their merge base. The checkout action defaults to depth 1, which cannot work.
          fetch-depth: 0

      - name: Use .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.x

      - name: Build BehaviorDiff CLI
        run: dotnet build src/BehaviorDiff.Cli/BehaviorDiff.Cli.csproj -c Release --nologo -v quiet

      - name: Analyze runtime behavior and measure
        id: analyze
        shell: bash
        run: |
          set +e
          mkdir -p "$(dirname "$BEHAVIORDIFF_FINDINGS")"
          cli="$GITHUB_WORKSPACE/src/BehaviorDiff.Cli/bin/Release/net8.0/behaviordiff.dll"
          metrics="$RUNNER_TEMP/behaviordiff-time.txt"
          stdout="$RUNNER_TEMP/behaviordiff.stdout.log"
          stderr="$RUNNER_TEMP/behaviordiff.stderr.log"

          /usr/bin/time -v -o "$metrics" \
            dotnet "$cli" "$GITHUB_WORKSPACE" --ci=github \
            --work "$BEHAVIORDIFF_WORK" --findings "$BEHAVIORDIFF_FINDINGS" \
            >"$stdout" 2>"$stderr"
          analysis_exit=$?
          cat "$stdout"
          cat "$stderr" >&2

          wall=$(awk -F': ' '/Elapsed \(wall clock\) time/{print $2}' "$metrics")
          peak_kb=$(awk -F': ' '/Maximum resident set size/{print $2}' "$metrics")
          trace_bytes=$(find "$BEHAVIORDIFF_WORK" -type f -name '*.ndjson' ! -name '*manifest*' \
            -printf '%s\n' 2>/dev/null | awk '{s+=$1} END {print s+0}')

          echo "HOSTED MEASUREMENT wall=$wall peak_rss_kb=$peak_kb traces_bytes=$trace_bytes exit=$analysis_exit"
          {
            echo "### BehaviorDiff hosted measurement"
            echo "- Wall clock: $wall"
            echo "- Peak RSS: $peak_kb KB"
            echo "- Trace bytes: $trace_bytes"
            echo "- Analysis exit: $analysis_exit"
          } >> "$GITHUB_STEP_SUMMARY"

          echo "exit=$analysis_exit" >> "$GITHUB_OUTPUT"
          echo "wall=$wall" >> "$GITHUB_OUTPUT"
          echo "peak_rss_kb=$peak_kb" >> "$GITHUB_OUTPUT"
          echo "trace_bytes=$trace_bytes" >> "$GITHUB_OUTPUT"

          if [[ ! -f "$BEHAVIORDIFF_FINDINGS" ]]; then
            cat > "$BEHAVIORDIFF_FINDINGS" <<JSON
          {
            "schema": "behaviordiff.findings/1",
            "status": "failed",
            "verdict": "could_not_analyze",
            "isCleanResult": false,
            "exitCode": $analysis_exit,
            "exitReason": "analysis_process_failed_without_artifact",
            "refs": { "baseSha": null, "prSha": null, "mergeBaseSha": null },
            "refusal": { "reason": "BehaviorDiff exited without findings.json. No safety verdict was produced; inspect the workflow logs." }
          }
          JSON
          fi

          # The posting step owns warn/fail gate semantics. Preserve every analysis state for it.
          exit 0

      - name: Post findings to pull request
        if: always()
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          if [[ ! -f "$BEHAVIORDIFF_FINDINGS" ]]; then
            echo "::error::findings.json is missing; no PR safety verdict or comment can be produced."
            exit 4
          fi
          dotnet src/BehaviorDiff.Cli/bin/Release/net8.0/behaviordiff.dll post \
            --provider=github --findings "$BEHAVIORDIFF_FINDINGS" --gate "$BEHAVIORDIFF_GATE"

      - name: Upload machine-readable findings
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: behaviordiff-findings
          path: ${{ env.BEHAVIORDIFF_FINDINGS }}
          if-no-files-found: error

      - name: Delete traces and worktrees
        if: always()
        shell: bash
        run: |
          before=$(du -sb "$BEHAVIORDIFF_WORK" 2>/dev/null | cut -f1 || echo 0)
          rm -rf "$BEHAVIORDIFF_WORK"
          echo "BehaviorDiff cleanup removed $before bytes; findings.json was retained as an artifact."
