<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <AssemblyName>behaviordiff-weaver</AssemblyName>
    <RootNamespace>BehaviorDiff.Weaver</RootNamespace>
    <!-- The descriptor pass reflection-loads the target, so the weaver must run on a runtime at least as
         new as the assembly it is weaving. -->
    <RollForward>LatestMajor</RollForward>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Mono.Cecil" Version="0.11.6" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\BehaviorDiff.Tracer\BehaviorDiff.Tracer.csproj" />
  </ItemGroup>

</Project>
