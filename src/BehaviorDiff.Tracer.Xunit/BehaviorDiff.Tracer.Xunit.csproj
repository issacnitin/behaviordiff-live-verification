<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <RootNamespace>BehaviorDiff.Tracer</RootNamespace>
    <AssemblyName>BehaviorDiff.Tracer.Xunit</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\BehaviorDiff.Tracer\BehaviorDiff.Tracer.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- xunit v2. v3 moved BeforeAfterTestAttribute to the Xunit.v3 namespace and changed the
         Before/After signature, so the integration is version specific and lives outside the core tracer. -->
    <PackageReference Include="xunit.core" Version="2.9.3" />
  </ItemGroup>

</Project>
